# BJMP Political Party Portal

The supplied design is split into:

- index.html — complete UI/content
- style.css — CSS/base styling
- app.js — lightweight interactions

The original Tailwind configuration is retained in index.html so the supplied utility classes reproduce the same UI.

External Google Fonts, Material Symbols, Tailwind CDN and image assets are retained from the supplied design.
